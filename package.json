{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "vercel-build": "composer install --prefer-dist --no-dev --optimize-autoloader"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    },
    "dependencies": {
        "@vercel/postgres": "^0.10.0"
    }
}
